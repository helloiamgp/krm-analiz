name: Build Windows EXE

on:
  push:
    tags:
      - 'v*'  # v1.0, v2.1 gibi tag'lerde çalışır
  workflow_dispatch:  # Manuel tetikleme

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Tesseract OCR
      run: |
        # UB-Mannheim Tesseract installer'ı indir
        $installerUrl = "https://digi.bib.uni-mannheim.de/tesseract/tesseract-ocr-w64-setup-5.3.3.20231005.exe"
        $installerPath = "$env:TEMP\tesseract-installer.exe"

        Write-Host "Tesseract indiriliyor..."
        Invoke-WebRequest -Uri $installerUrl -OutFile $installerPath

        Write-Host "Tesseract kuruluyor..."
        Start-Process -FilePath $installerPath -ArgumentList "/S" -Wait

        Write-Host "PATH güncelleniyor..."
        echo "C:\Program Files\Tesseract-OCR" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

        Write-Host "Tesseract kurulumu tamamlandı!"
      shell: pwsh

    - name: Verify Tesseract installation
      run: |
        if (Test-Path "C:\Program Files\Tesseract-OCR\tesseract.exe") {
          & "C:\Program Files\Tesseract-OCR\tesseract.exe" --version
          Write-Host "✓ Tesseract başarıyla kuruldu!"
        } else {
          Write-Host "✗ Tesseract bulunamadı!"
          exit 1
        }
      shell: pwsh

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Build EXE with PyInstaller (Tesseract dahil)
      run: |
        pyinstaller --onefile --name="KRM-Analiz" `
          --add-data="fonts;fonts" `
          --add-binary="C:\Program Files\Tesseract-OCR\tesseract.exe;." `
          --add-data="C:\Program Files\Tesseract-OCR\tessdata;tessdata" `
          --hidden-import=openpyxl `
          --hidden-import=requests `
          --console --clean krm.py

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: KRM-Analiz-Windows
        path: dist/KRM-Analiz.exe

    - name: Upload to GitHub Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v2
      with:
        files: dist/KRM-Analiz.exe
        tag_name: ${{ github.ref_name }}
        name: ${{ github.ref_name }}
        draft: false
        prerelease: false
        body: |
          ## 🎯 KRM Analiz Aracı v${{ github.ref_name }}

          ### 📥 İndirme
          **KRM-Analiz.exe** (90 MB) - Tesseract OCR ve Logo Çekme Dahil

          ### ✨ Özellikler
          - ✅ Tesseract OCR dahil - Findeks eşleştirmesi çalışır
          - ✅ Otomatik logo indirme - İlk çalıştırmada
          - ✅ Tamamen standalone - Kurulum gerektirmez
          - ✅ Klasör bazlı analiz - Alt klasörleri tarar

          ### 🚀 Kullanım
          1. **KRM-Analiz.exe** dosyasını indirin
          2. İstediğiniz klasöre kopyalayın
          3. KRM PDF'lerini aynı klasöre/alt klasörlere koyun
          4. **KRM-Analiz.exe**'yi çift tıklayın
          5. Raporlar `output/` klasöründe oluşur

          ### ⚠️ Önemli Notlar
          - İlk çalıştırmada Windows Defender uyarısı alabilirsiniz
          - "Yine de çalıştır" seçeneğini tıklayın
          - Logo indirme için: `2025-11-09_bankalar_listesi.xlsx` gerekli (opsiyonel)

          ### 🔧 Sistem Gereksinimleri
          - Windows 10/11 (64-bit)
          - En az 200 MB boş disk alanı
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
